# offline = fixtures only, no network (default for `make demo`); live = query data sources
DEMO_MODE=offline

# None of these is required. The offline demo and the live no-auth mirrors run without them.
# See credentials.md for how to obtain each one.
CDSE_CLIENT_ID=
CDSE_CLIENT_SECRET=
EARTHDATA_TOKEN=
USGS_TOKEN=
